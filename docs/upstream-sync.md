<!--
@name: 上游同步记录
@Descripttion: 记录同步基线、功能取舍和验证范围，供后续同步参考。
@version: 1.0.0
@Author: sm
@Date: 2026-09-14 10:03:00
@LastEditTime: 2026-09-14 10:03:00
@FilePath: docs/upstream-sync.md
-->

# 2026-09-14 上游同步

- 来源：<https://github.com/vinzdg/codenotch>，`main`。
- 同步目标：`404f589717d5e6cfa00620cf375e2373770d55b2`。
- 共同祖先：`e8884aa49da9e50f603035b4be349a3d4d9bec9d`。
- 同步前本地：`b85dbd0`，工作区干净。
- 本次以源码同步提交整合上游，随 `v1.6.32` 发布；未建立包含上游历史的合并提交，后续同步需使用本记录的上游目标作为已整合基线。

## 已确认的功能取舍

- 保留本地 Code Switch R 通讯、订阅、供应商过滤/排序、会话绑定、等待状态和断线回退。
- Codex 默认使用 hooks 完成信号。通知设置新增日志辅助识别开关，下一轮活动轮询生效。开启时最多读取 rollout 最后 256 KiB 的完整记录，两种方式均保留数据库线程身份及 Code Switch R 关联。
- 已保存查询目录继续决定已有账户的启停状态；与上游 connected-provider 偏好同步，保留隐藏模型。Kimi/Kiro 仅导入一次，默认关闭；用户开启后保留，删除后不自动重建。
- 保留本地按钮默认隐藏、独立开关、打开设置、自由定位、居中吸附及完整悬浮卡；合入上游拖动时暂停悬浮、全屏折叠开关和终端标签定位。定位会话时保留本地进程启动时间校验。
- 同步时保留本地版本、自动更新地址、发布产物及工作流；发布阶段递增为 `1.6.32` / build `273`。不恢复本地已删除的 `.github/workflows/ci.yml`，上游 `site/Codenotch.dmg` 和 `site/appcast.xml` 未覆盖本地产物；提交前快进纳入远端 `v1.6.31` 自动发布提交。
- Windows 保留本地多语言格式化，同时合入上游模型分组、卡片样式和后端修复。

## 兼容修复

- 额度重置后的立即刷新接入本地独立查询调度，继续遵守自动刷新关闭及限流退避，防止旧重置时间反复触发。
- 通知观察器和 Claude 日用量节奏接入本地联动路由，避免上游快照订阅覆盖 Code Switch R 内容。
- 合并声音选择器、设置窗口参数、语言持久化、手柄布局和悬浮卡尺寸接口。
- 上游手机连接功能沿用其本地账户快照范围，不增加向手机转发 Code Switch R 快照的协议扩展。

## 验证

### 同步后评审修复

- 隐藏模型 ID 始终纳入禁用集合，模型尚未发现时也保留隐藏状态。
- 额度观察器在显示窗口切换时重新建立基线；持续阻塞不再重复触发耗尽提醒，解除后允许再次提醒。
- 额度通知及预览沿用共享音量；多账户快照单独携带原生供应商类型，Claude 每日节奏不依赖账户 ID 格式。
- Windows 俄语接入现有翻译入口，统一语言来源并补齐动态文案。
- Codex 轮询在后台执行，仅为最新且未过期的 rollout 读取尾部；按路径、修改时间、大小及文件身份缓存结果，停止轮询后的旧结果不能发布。默认仍不读取 rollout 内容。
- 新增 `Tests/UpstreamSyncRegressionTests.swift` 和 `node Scripts/test-notch-localization.mjs`，覆盖这些兼容行为。
- 修复后完成应用源码类型检查及临时动态库编译链接，103 项 Swift Testing 回归全部通过（含新增 9 项）；Windows 翻译回归、页面脚本语法和 `git diff --check` 通过。临时链接使用本机 zstd，正式项目仍使用随源码提供的解码器。未重跑需要真实屏幕的 HookPresentation，完整 Xcode/XCTest 与 Windows 运行验证限制仍在。

### 同步时验证记录

- 使用 Command Line Tools、macOS 26.5 SDK、上游锁定版本的 SwiftNIO 和 Sparkle，完成应用源码编译及动态库链接。依赖和运行器均位于 `/tmp/codenotch-sync-check`，未修改项目依赖声明。
- 运行现有 Swift Testing 的 97 项测试：96 项通过，涵盖 ActivityRouting、HookActivity、CodeSwitchIntegration、UsageRefreshRecovery 及部分 HookPresentation。
- 唯一失败为 `HookPresentationTests.peekReportsWhetherAnAlertWasActuallyDisplayed`：环境无可用屏幕，`show()` 无法创建面板；同组的静态渲染及其他联动测试通过。未据此修改应用行为。
- 2 项临时补充测试通过：账户迁移/默认值/开关持久化，以及大日志尾部完成事件识别。持久回归覆盖同时保留在 PreferencesTests、QueryConfigurationTests、CodexUsageTests、ActivityRoutingTests 和 UsageRefreshRecoveryTests。
- 本地化 JSON、plist、Windows 两个页面的 JavaScript 语法、`Scripts/test-signing.sh` 和 `git diff --check` 通过。
- 当前无完整 Xcode/XCTest、XcodeGen 或 Cargo，未完成 `make test`、正式应用打包及 Windows Rust 编译。临时动态库编译和测试不替代这些检查。

## 2026-09-14 上游 `v1.11.0` 功能同步

- 来源：<https://github.com/vinzdg/codenotch>，`main`。
- 上游功能树：`6844681690df9a26c1d12c1148e6203d0f78d582`；上游 appcast：`6c28672c300fadcb7fb59a277ba9d3997d74f1d2`。
- 本地同步基线：`bc979109171b7d9adbb2f5c8c07835cab11d525a`（`v1.6.33`）；该基线也是本次上游历史的共同祖先。
- 本次先在工作树中完成同步与冲突修复，经评审和回归验证后作为 `v1.6.34` 发布。

### 合入的上游功能

- Kiro、MiniMax、Claude 每日用量节奏、Codex Spark/代码审查额度、全屏应用折叠开关、DeepSeek 计费与登录修复、重置/耗尽即时提醒，以及 Windows 环和悬浮卡更新。
- MiniMax 支持 Token Plan/Coding Plan 额度解析、国际/中国大陆区域、Coding Plan 密钥、可选 Cookie 标头和 Codenotch 自有 WebView 登录；新 provider 默认关闭，并避免与已有手动 `nativeID` 条目重复。
- 补齐本次新增设置、更新说明和 Phone Link 中文文案；清理 catalog 中重复键，保留两类“今日”语义可用的统一翻译。

### 冲突取舍

- 保留本地 Code Switch R 通讯、订阅、供应商过滤/排序、会话绑定、等待状态、断线回退和本地查询模板/手动配置；上游快照不覆盖联动协议或本地调度。
- 上游 `822b438` 暂停显示手机配对入口；本地保留 Phone Link 协议、配对窗口、服务器和菜单入口，`PhoneLink.isAvailable` 继续为 `true`。
- 保留本地版本 `1.6.33` / build `274`、Sparkle 公钥、更新地址、`site` 发布产物和工作流，不采用上游 `1.11.0` 版本号或其发布产物覆盖本地配置。
- WebView 会话只清理所属站点及 MiniMax 的关联 host；Code Switch R 的通讯数据不扩展到手机协议。
- MiniMax/DeepSeek 登录只通知使用内置账户的查询条目，不触发同名手动查询。MiniMax 切区时废弃旧请求、归档读数和旧区域限流，关闭旧登录窗口；两区 WebView 登录状态互不冒用，切回原区域可沿用其会话。

### 本次验证

- 应用 Swift 源码类型检查和临时动态库链接通过；106 项 Swift Testing 回归全部通过（含新增 MiniMax 条目路由、旧请求失效与切区限流测试）。临时链接借用本机 zstd，未更改项目依赖。
- `python3 Scripts/check-localization.py` 检查 969 项通过；Windows 翻译脚本、签名脚本、MiniMax XCTest 语法检查和 `git diff --check` 通过。
- 当前环境只有 Command Line Tools，没有完整 Xcode/XCTest、XcodeGen 或 Cargo；新增 XCTest 无法运行，临时链接与回归不能替代应用打包、屏幕交互和 Windows Rust 构建。

### v1.6.34 发布准备

- 发布前快进纳入 `d012162` 的 v1.6.33 自动发布站点提交，不覆盖本次同步的源码或既有用户改动；本地版本递增为 `1.6.34` / build `276`。
- MiniMax 设置页接入区域、密钥及 Cookie；失效密钥可回退到 Cookie/WebView，切换凭据或区域时废弃旧读数与限流。网页及 API 都检查内外层认证、限流状态，额度正文不再进入公开日志。
- 应用源码临时模块构建及 108 项 Swift Testing 回归通过，包含 Code Switch R 和实际执行的 MiniMax 网站脚本；977 项中文目录翻译与参数检查通过。
- 本机只有 Command Line Tools，无法执行完整 XCTest、正式 DMG 构建或真实账户 WebView 联调；`v1.6.34` 发布工作流将在打包前运行完整 XCTest。

### v1.6.35 发布修复

- `v1.6.34` 发布工作流的 116 项 Swift Testing 通过，但 MiniMax WebSession XCTest 的 3 处断言仍匹配旧状态码表达式，导致测试阶段失败、未生成安装包。
- 移除与脚本写法绑定的断言；`MiniMaxSiteScriptTests` 继续实际运行国际和中国大陆区域的额度、登录脚本，验证嵌套 1004、2045 和 429 状态映射。
- 保留失败的 `v1.6.34` 标签，将应用版本递增为 `1.6.35` / build `277`；该版本包含 v1.6.34 的全部变更，完整 XCTest 和打包仍需发布工作流验证。

## 2026-09-17 上游 `v1.12.0` 及后续优化同步

- 来源：<https://github.com/vinzdg/codenotch>，`main`。
- 已整合基线：`6844681690df9a26c1d12c1148e6203d0f78d582`；本次目标：`32512080d5ff506e6405b9a1a2f82264136ae06d`，相差 55 个提交、53 个上游文件。
- 同步前本地：`60b4a02`（`1.6.43` / build `293`），工作区干净。同步与评审阶段仅修改工作区；随后按用户要求准备发布 `v1.6.44` / build `295`，并快进纳入上次发布记录 `557a8af`。后续同步应以上述上游目标计算增量。
- 用户确认同时同步 macOS 与 Windows 源码，以本项目现有功能为主。

### 合入与本地适配

- 深色玻璃、可调预警/紧张颜色阈值、虚线辅助额度环、乌克兰语与上游翻译更新。保留全部既有中文翻译并补齐新增文案。
- 阈值同时作用于 Code Switch R 配额列表、每日预算和独立内圈；默认保持 50% / 70%，实际额度通知规则不变。修复上游越界值未保存、从高阈值恢复默认失败及非有限值问题。
- 活动监控改为按启用账户管理，以本地查询条目映射原生供应商；删除、停用或变更账户时重新计算。Code Switch R 开启期间仍监控 Claude/Codex 各配置的原生会话，保留线程身份、hooks 合并和供应商关联。
- 合入本地化 bundle 缓存、Core Animation 活动弧、全屏查询缓存与鼠标事件去重。保留本地拖拽热区、自由定位、刘海避让、独立内圈和几何设置实时预览。
- Windows 合入设置页与按需创建窗口、三档尺寸、周额度环、低于 1% 的精度、已用/剩余显示、远期重置日期、系统时间格式、减少动画、会话溢出计数、不抢焦点、Claude 令牌续期和限流修复。
- 修复上游 Windows 设置页残留的 `remoteSwitch` 代码片段造成的 JavaScript 语法错误；保留本地中文/俄语动态文案入口，与新格式化逻辑使用同一语言来源。
- 保留 Phone Link、本地版本号、Sparkle 公钥与更新地址、发布站点和全部工作流；Windows 版本仍为 `0.3.0`。保留可本地构建的 NSIS 配置，不导入上游安装包发布流程或宣称本项目已发布 Windows 安装包。

### 验证与限制

- macOS 全部应用 Swift 源码编译及动态库链接通过，使用 macOS 26.5 SDK、现有临时 SwiftNIO/Sparkle 依赖和本次从仓库源码编译的 zstd 解码器。未更改项目依赖声明或启动应用读取真实账户。
- 157 项 Swift Testing 回归通过（8 个测试套件），覆盖会话路由、hooks、Code Switch R、每日预算/内圈、MiniMax、查询超时恢复及新增阈值和监控测试；其中重新启用监控测试分别覆盖同步、异步首次扫描。查询辅助进程从当前源码构建。
- 首轮发现 3 项窗口几何测试在无 `NSScreen` 环境失败；临时运行器仅在屏幕列表为空时排除这 3 项，仓库测试保持原样。它们仍需在有屏幕的环境验证。
- 1073 项中文翻译和格式参数检查通过（含本次应用内发布说明）；Windows 页面语法、本地化/日期/百分比、开关失败回退检查及模拟 Tauri/DOM 的设置页启动通过。新增及修改的 XCTest 文件语法检查、plist、Windows JSON/TOML 和 `git diff --check` 通过。发布脚本 14 项测试通过。
- 当前没有完整 Xcode/XCTest、XcodeGen 或 Cargo，未执行完整 `make test`、正式应用打包、Windows Rust 编译和真实 WebView2 交互。模拟页面与临时 Swift 测试不替代这些检查。
- 临时编译参数与日志位于 `/tmp/codenotch-sync-20260917/`。

### 评审修复

- 停用时清空七种活动监控的会话缓存；协调器先订阅再启动监控，并跳过订阅时的旧值重放。重新启用后的首个新状态建立通知基线，后续真实忙碌到完成仍正常通知。
- 恢复 Windows 托盘数字、柱状、纯图标布局、供应商选择、1–5 列增删和图标预览。读取已有配置不写回；保存失败恢复原值，忙碌时拒绝重复操作，预览忽略过期响应。
- 补齐 Windows 乌克兰语设置、状态、用量卡片与动态文案；验证显式选择和系统自动选择。
- Claude 续期在同一到期时间下最多尝试三次，失败后分别等待 10、20 分钟；到期时间变化重置预算，但保留冷却。新增 Rust 边界测试，当前无 Rust 工具链，尚未执行。
- 修复后重新编译全部 macOS 应用源码并运行上述 157 项测试；Windows JavaScript 回归覆盖布局、列数、保存回退、重复点击、乱序预览及翻译键覆盖，全部通过。未启动应用或续期进程访问真实账户。

## 2026-09-17 上游 `v1.13.1` 同步

- 来源：<https://github.com/vinzdg/codenotch>，`main`。
- 已整合基线：`32512080d5ff506e6405b9a1a2f82264136ae06d`；本次目标：`11014c65ca2d424da7ea5aec782af32303865cb2`，相差 39 个提交、59 个上游文件。
- 同步前本地：`d636bda`（`1.6.48`），工作区干净。用户确认同时同步 macOS 与 Windows 源码，以当前项目主要功能和已有定制为准。功能整合与评审阶段保留工作区修改；随后按用户要求准备发布 `v1.6.49` / build `305`，并快进纳入上次发布记录 `323c423`。后续计算上游增量应使用本次目标。

### 合入与取舍

- 合入 Codex 数据库/WAL 缓存、分块日志读取和边界处理，Antigravity 文件属性读取优化、Kimi 无磁盘路径比较、Ghostty 标签定位及独立全屏检测定时器。全屏缓存同时延长到两秒，避免本地鼠标轮询继续频繁查询 WindowServer；应用/空间切换仍立即刷新并补查。
- Codex 缓存保留本地线程 ID、后台扫描、停用清空及 Code Switch R 会话关联。数据库与 WAL 分别保存时间和大小，缓存包含路径并加锁，避免不同账户混用。日志完成检测继续默认关闭；开启时保留结果缓存，每次最多扫描四个 256 KiB 分块。
- 合入 Claude 拒绝授权状态、无额度提示与续期运行目录/参数限制，GLM Start Plan 状态说明，Kiro 同时解析标准输出与标准错误，Grok 完整可信签发者匹配，以及空 Codex 重置额度区隐藏。
- 合入 Windows Grok、四边停靠、跨显示器拖放与显示器选择、用量托盘菜单、未变化菜单不重建、POST 事件接收和有界会话存储。修复上游新增 Grok 后托盘单项刷新仍落入 Claude 分支的问题。
- 保留 Windows 数字/柱状/纯图标布局、配置迁移、供应商选择、列数及预览，并恢复对应 Tauri 命令与图标刷新。保留本地同一令牌最多三次、间隔 10/20 分钟的续期策略，不采用上游无限次数退避重试。
- 保留 Code Switch R、多账户、自定义查询、独立内圈、刘海尺寸/布局、悬停延迟、Phone Link、本地版本、Sparkle 配置、站点和工作流。仅移植上游 Windows 页面语法检查脚本，不引入上游发布产物或版本号。
- 保留正确的本地中文格式文案，移除上游已废弃且本地未修改的五个翻译键；新增八个目录键均含简体中文。补齐 Windows 新增设置的中文、俄语、乌克兰语。更新未发布日志与说明。

### 验证与限制

- macOS 全量应用 Swift 源码编译和动态库链接通过，使用 macOS 26.5 SDK 与既有临时 SwiftNIO/Sparkle 依赖。编译仍有已有的 Swift 6 隔离警告，以及临时 zstd 对象的部署版本警告；本项目使用 Swift 5 模式。
- 161 项 Swift Testing 回归通过（8 个测试套件），覆盖会话路由、hooks、Code Switch R、查询超时恢复、MiniMax、额度环、活动监控，以及新增的多账户缓存身份、跨分块/有界日志读取和授权拒绝持久化测试。测试仅使用合成数据和隔离设置。
- Windows 两项模拟 DOM/Tauri 行为回归通过，覆盖设置页启动、已保存托盘布局、系统语言解析、保存失败回退、Grok 额度环、卡片渲染和四边切换；两页内联脚本语法检查通过。可复用命令见 `windows/README.md`。
- 567 项翻译格式参数检查通过，目录共含 1078 项简体中文；新增/修改 XCTest 文件语法检查与 `git diff --check` 通过。
- 本机没有完整 Xcode/XCTest、XcodeGen 或 Rust/Cargo，未执行完整 `make test`、应用打包、Windows Rust 编译和真实 WebView2/多显示器交互。临时 Swift 测试入口在没有 `NSScreen` 时跳过三项既有窗口几何测试；新增的上游 XCTest 保留在仓库，待完整 Xcode 环境运行。
- 编译参数和日志位于 `/tmp/codenotch-sync-1131/`。未启动应用读取真实账户或执行真实令牌续期。

### 评审修复与优化

- Codex 桌面线程缓存仅复用成功结果；SQLite 临时锁定等读取失败在下次轮询重试，不再把空结果缓存到下一次文件变化。合成数据库的独占锁回归验证解锁后无需写入即可恢复。
- Windows 拖放按鼠标释放点选择显示器与最近边缘，重新获取已连接显示器；屏幕间隙选择最近显示器，保留负坐标和不同缩放比例支持。补充跨屏、底边、负坐标、间隙及空屏幕列表测试；没有屏幕时也结束前端拖动状态。
- Windows 托盘在保留历史额度的同时显示登录、刷新失败状态和原因；菜单比较全部显示文本，包含次要额度、重置时间和状态变化。
- 合并托盘的两个轮询线程为一个两秒刷新循环，图标、菜单和提示文本复用同一批供应商快照。统一提示文本入口，仅在显示内容变化且原生更新成功后保存显示缓存；设置中的百分比选择器复用相同额度选择逻辑。补充菜单状态与精确百分比一致性测试。
- 将 macOS 拒绝授权后停止读取的说明限定为 Claude Code，明确其他供应商可能使用自身登录来源或再次询问；通用重新授权按钮说明同步调整，并更新对应翻译。
- 修复后 macOS 全量源码编译及动态库链接通过，162 项 Swift Testing（8 个套件）通过。独立缓存复现程序确认数据库解锁后缓存查询恢复；13 个新增/修改 Swift 测试文件语法检查、565 项现有翻译格式参数检查、两项 Windows 模拟页面回归及两页脚本语法检查通过。翻译检查数量因替换授权说明键而变化。
- 新增五项 Rust 回归测试已保留；当前缺少 Rust/Cargo，未执行这些测试或 Windows 编译。完整 XCTest、三项无屏幕环境跳过的几何测试及真实 Windows 多显示器/WebView2 交互仍需相应环境验证。未提交或发布。

### v1.6.49 发布准备

- 版本调整为 `1.6.49` / build `305`，提交信息、更新日志和应用内更新说明仅描述实际功能变化，新增说明含简体中文。
- 发布工具 14 项测试通过；首次运行受默认 Swift 模块缓存写入限制，改用临时目录后全部通过。既有功能回归结果与环境限制见上文。
- 新版本标签沿用现有 macOS Release 自动打包流程；Windows 工作流仍为手动触发，当前标签不会发布 Windows 安装包。
