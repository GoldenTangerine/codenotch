<!--
@name: 上游功能同步记录
@Descripttion: 记录移动手柄开关的上游来源、本地适配与验证边界。
@version: 1.0.0
@Author: sm
@Date: 2026-09-12 11:42:44
@LastEditTime: 2026-09-12 11:42:44
@FilePath: docs/plans/2026-09-12-upstream-sync.md
-->

# 上游同步：2026-09-12

- 本地起点：`d0e0ff55bc326e40959eb902e7f2767723f46dc0`，工作区干净。
- 上游：`vinzdg/codenotch` 的 `main`，检查时为 `25e1a081fd18bc9360698ec852814b6d057d14c5`。
- 上次同步点 `e8884aa49da9e50f603035b4be349a3d4d9bec9d` 之后新增三个提交。本次移植功能提交 `4a596ea`（Add a setting to hide the move handle）；其余两个为上游 1.9.0 发布内容，不移植。

## 适配

新增外观设置“显示移动手柄”，保存开关并同步所有显示器，包括后续创建的窗口。隐藏时同时撤去图形和命中点；切换时重新布局并刷新交互区域。

上游默认显示手柄，本仓库原先将入口移入右键菜单，因此本次保留默认隐藏。开启后只显示移动手柄，设置入口继续使用右键菜单。定位布局为开启的手柄预留空间，关闭后恢复本地布局。补充简体中文、使用说明和持久化/渲染回归用例。

保留本仓库版本号、Sparkle 更新源、发布产物和所有 GitHub 工作流。发布仍由推送 `v*` tag 触发，沿用现有测试、打包和 Latest 版本检查。本次只形成未提交修改，没有暂存、提交、打 tag、推送或发布，也没有更改远程配置。

## 验证

- 复用临时 SwiftPM 检查工程，以 macOS 26.5 SDK 完成应用源码编译链接。
- 9 个修改的 Swift 文件完成语法检查；805 个本地化条目的中文与格式参数检查通过。
- `git diff --check` 通过；`.github`、`Makefile`、`project.yml` 和 `site` 没有改动。
- 新增 XCTest 覆盖默认隐藏、开关双向持久化，以及手柄隐藏后的绘制和命中区域。

本机只有 Command Line Tools，缺少完整 Xcode/XcodeGen，未运行正式 XCTest 或应用打包。默认 macOS 27 SDK 缺少所需 SwiftUI 宏插件，补充编译使用可用的 26.5 SDK；编译日志位于 `/private/tmp/codenotch-upstream.dLDD7o/build26.log`。未启动应用访问真实账户。

## 评审修复

- 手柄窗口命中框随显示比例缩放，与绘制及模型命中范围一致。
- 普通偏移、保存位置和完整气泡布局都按缩放后的实际手柄尺寸保留前端空间；隐藏后撤去对应留白。原有设置入口的后端留白独立处理。
- 成功查询的全量缓存编码合并到 250 毫秒窗口，不随连续响应无限延期。停用、注销、配置失效和退出立即保存当前有效读数并取消待写任务。异常退出最多损失该窗口内尚未持久化的最新读数。
- 新增四边定位、完整气泡前端留白、150% 实际窗口命中、缓存批量保存及注销/退出回归测试。原有断开连接测试改为等待缓存持久化后检查成功读数，清除缓存的断言仍立即检查。

macOS 26.5 SDK 应用编译链接、16 个修改 Swift 文件的语法检查、805 项本地化检查和差异格式检查通过。独立行为验证使用模拟供应商和隔离偏好，验证四边保存/普通布局、完整气泡留白、集中响应只保存一次、注销后不恢复旧读数以及退出后无重复写入。沙箱无法访问 WindowServer，获自动审批后在沙箱外运行同一验证程序，四边 150% 实际面板命中及隐藏后释放命中区域均通过；测试模式未显示窗口或访问真实账户。

验证程序：`/private/tmp/codenotch-upstream.dLDD7o/FixProbe.swift`；编译日志：`review-fix-build.log`（同目录）。正式 XCTest 和发布打包仍需完整 Xcode；GitHub tag 发布工作流未改动。

## v1.6.26 发布准备

用户已授权编写提交及更新日志、提交代码并推送新 tag 触发 GitHub 自动打包。版本设为 `1.6.26 / 263`，同步维护 `CHANGELOG.md`、应用内更新记录和中文翻译。合并远程 `ed462f4`（上次发布自动生成的 site 产物提交），保留远程历史。继续使用现有 `v*` tag 发布工作流；本地验证不替代云端完整 XCTest 和 DMG 构建结果。
